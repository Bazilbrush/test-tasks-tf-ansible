apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "queues.fullname" . }}-test-connection"
  labels:
    {{- include "queues.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "queues.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
